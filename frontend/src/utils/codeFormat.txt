#include <Adafruit_GFX.h> // Core graphics library
`Hardware-specific library import`
`Custom font imports`
/* Color definitions */
`Custom color definitions`

/* IMPORTANT: You must initialize the library yourself using correct pins */
`Hardware-specific library initialization`

void displayOutput() {
  `Code output from editor`
}
 
void setup(void) {    
  displayOutput()
}