#include <Adafruit_GFX.h>    // Core graphics library
`Hardware-specific library import`
`Custom font imports`

// Color definitions
#define BLACK    0x0000
#define BLUE     0x001F
#define RED      0xF800
#define GREEN    0x07E0
#define CYAN     0x07FF
#define MAGENTA  0xF81F
#define YELLOW   0xFFE0 
#define WHITE    0xFFFF
`Custom color definitions`

lib = "INITIALIZE HARDWARE SPECIFIC LIBRARY HERE WITH CORRECT PINS"

void displayOutput(lib) {
  `Code output from editor`
}
 
void setup(void) {    
  /**START DISPLAY HERE**/
  displayOutput(lib)
}